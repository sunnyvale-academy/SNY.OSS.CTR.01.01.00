apiVersion: apps/v1
kind: Deployment
metadata:
  name: sender-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: sender-app
  template:
    metadata:
      labels:
        app: sender-app
    spec:
      initContainers:
        - name: init-wait-for-rabbitmq
          image: busybox
          command: ['sh', '-c', 'until nc -z rabbitmq 5672; do echo "waiting for rabbitmq"; sleep 2; done;']
      containers:
      - name: sender-app
        image: dennydgl1/rabbitmq-sender:1.2
        imagePullPolicy: Always
        env:
          - name: RABBITMQ_USERNAME
            valueFrom:
              secretKeyRef:
                name: sender-secret
                key: username
          - name: RABBITMQ_PASSWORD
            valueFrom:
              secretKeyRef:
                name: sender-secret
                key: password